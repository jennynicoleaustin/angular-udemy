export class Ingredient {
  // Shorthand -> automatically creates the fields, and assigns the fields within the constructor based on the parameters passed.
  constructor(public name:string, public amount: number) {}

}
